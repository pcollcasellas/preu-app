# Database Configuration
# Required: PostgreSQL connection string for your database
# Example for Neon: postgresql://username:password@ep-xxx-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
# Example for local: postgresql://user:password@localhost:5432/supermarket_db
DATABASE_URL=postgresql://username:password@your-database-host/database_name?sslmode=require

# Environment Configuration
# Options: dev, staging, prod
ENVIRONMENT=dev

# Bonpreu Scraper Configuration
# Base URL for Bonpreu online store
BONPREU_BASE_URL=https://www.compraonline.bonpreuesclat.cat
# Sitemap URL for product discovery
BONPREU_SITEMAP_URL=https://www.compraonline.bonpreuesclat.cat/sitemaps/sitemap-products-part1.xml

# Mercadona Scraper Configuration
# Base URL for Mercadona online store
MERCADONA_BASE_URL=https://tienda.mercadona.es
# Sitemap URL for product discovery
MERCADONA_SITEMAP_URL=https://tienda.mercadona.es/sitemap.xml
# API endpoint for product details
MERCADONA_API_URL=https://tienda.mercadona.es/api/products

# Scraping Configuration
# Fraction of products to process in each batch (default: 1/48 = ~2% of products)
BATCH_SIZE_FRACTION=0.020833333
# Duration in minutes for each batch processing cycle
BATCH_DURATION_MINUTES=10
# Number of concurrent HTTP requests during scraping
CONCURRENT_REQUESTS=15
# Timeout in seconds for HTTP requests
REQUEST_TIMEOUT=30

# API Configuration
# Host address for the API server
API_HOST=0.0.0.0
# Port number for the API server
API_PORT=8000

# Optional: Logging Configuration
# LOG_LEVEL=INFO
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Optional: Rate Limiting Configuration
# MAX_REQUESTS_PER_MINUTE=60
# RATE_LIMIT_BURST=10
